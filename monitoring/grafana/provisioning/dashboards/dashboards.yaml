apiVersion: 1

providers:
  - name: "gpu-llm"
    orgId: 1
    folder: "GPU LLM"
    type: file
    disableDeletion: false
    editable: true
    options:
      path: /var/lib/grafana/dashboards/gpu-llm